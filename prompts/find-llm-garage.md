---
description: Locate the llm-garage repo on this machine and explain what it's for.
argument-hint: ""
---
Find where the `llm-garage` repo lives on this computer (check common locations like
`~/Documents/Code/`, `~/code/`, `~/dev/`, `~/projects/`, or search for a directory
containing a `sync/generate.py` and `context/GLOBAL.md`). If you can't find it, say so.

Once found, read its `README.md` and report back:
- The absolute path to the repo.
- In your own words, what the repo is for and how it fits into this machine's setup
  (source of truth for skills/subagents/prompts/context synced across Claude Code,
  Codex CLI, OpenCode, and pi).
- Whether it looks like it's currently synced on this machine (e.g. check for symlinks
  such as `~/.claude/CLAUDE.md` pointing into the repo).

Keep the report under 15 lines.
